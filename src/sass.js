import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  screenMainContainer: {
    borderColor: 'red',
    borderWidth: 1,
    flex: 1,
    width: '100%',
    padding: '10%',
  }
});

export default styles;

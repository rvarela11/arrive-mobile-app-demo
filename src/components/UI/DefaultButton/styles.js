import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
      alignItems: 'center',
      borderRadius: 2,
      margin: 5,
      padding: 10,
  },
  buttonWithIcon: {
      flexDirection: 'row',
  },
  disabled: {
      backgroundColor: '#eee'
  }
})

export default styles;

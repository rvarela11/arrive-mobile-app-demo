import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
      padding: 10,
      margin: 5,
      borderRadius: 2,
      alignItems: 'center'
  },
  disabled: {
    backgroundColor: '#eee'
  }
})

export default styles;
